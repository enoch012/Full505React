<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bitc.mapper.UserMapper">
  <!-- 유저 리스트 불러오기 -->
  <select id="selectUserList" resultType="com.bitc.dto.UserDto">
    SELECT user_id, user_pw, user_name, user_email
    FROM user
  </select>
  <insert id="addUser" parameterType="com.bitc.dto.UserDto">
    INSERT INTO
    user (user_id, user_pw, user_name, user_email)
    VALUES
    (#{userId}, #{userPw}, #{userName}, #{userEmail})
  </insert>
</mapper>